# 墨韵书斋功能更新 - 2026-05-17

## 本次更新内容

### 1. 作品集预览功能
- 在作品集页面，点击任意作品卡片，弹出全屏预览模态框
- 预览显示：作品图片、书体类型标签（硬笔=蓝色、毛笔=金色）、书写原文、AI赏析
- 关闭方式：点击右上角 ✕ 按钮，或点击遮罩层

### 2. 录入页添加硬笔/毛笔选项
- 在录入页的「选择作品集」下方，新增「书体类型」下拉框
- 可选：毛笔、硬笔（默认毛笔）
- 保存作品时，`brushType` 字段会存入 IndexedDB
- 作品集列表和预览页都会显示书体类型标签

### 3. AI 赏析结合书体类型 + 书写内容
- AI 调用时，会将「书体类型」和「书写内容」一起发送给 API
- 系统提示词已更新，要求 AI 结合硬笔/毛笔的书写工具特点进行分析
- 导出的 Word 文档中，图片页后会标注【书体类型】

## 修改的文件

### `index.html`
- 修复了之前被错误编辑导致的重复「书体类型」选择框问题
- 在录入页添加了书体类型选择框（位于「选择作品集」下方）
- 添加了预览模态框的 HTML 结构（`.preview-overlay`）
- 添加了预览相关的 CSS 样式

### `app.js`
- `DB` 对象：新增 `getWorkById()` 方法，根据 id 获取单个作品
- `App.aiAnalyze()`：更新 prompt，将书体类型和内容一起发送；系统提示词要求 AI 结合书体类型分析
- `App.save()`：保存时读取 `#penType` 的值，存入 `brushType` 字段
- `App.loadWorks()`：生成作品列表 HTML 时，为每个 `.work-item` 添加 `onclick="App.previewWork(id)"`；在标题前添加书体类型标签
- `App.previewWork(id)`：新增方法，根据作品 id 从 IndexedDB 读取完整信息，填充预览模态框并显示
- `App.closePreview()`：新增方法，关闭预览模态框
- `initApp()`：添加预览关闭按钮和遮罩层点击关闭的事件监听
- `Docx.gen()`：在生成 Word 文档时，如果作品有 `brushType` 字段，在图片页后添加一行「【书体类型】」

## 数据库变更说明
- IndexedDB 的 `works` 对象存储没有显式定义 `brushType` 字段，但 IndexedDB 是对象存储，可以存储任意属性
- 旧作品没有 `brushType` 字段，在预览和导出时会自动处理（不显示标签或不输出书体类型行）
- 无需升级数据库版本

## 使用建议
1. 刷新浏览器（建议硬刷新 Ctrl+F5）以加载新代码
2. 新录入作品时，记得选择书体类型
3. 在作品集页面点击作品体验预览功能
4. AI 赏析现在会结合书体类型，结果会更精准

## 已知问题
- 旧作品没有 `brushType` 字段，预览中不显示书体类型标签，但不影响其他功能
- 如果需要为旧作品添加书体类型，需要手动重新保存（先删除再重新录入）
